# AIGC 标注项目管理工具

一个可直接部署到 GitHub Pages 的静态网页工具。

## 使用方式

打开 `index.html` 即可使用。部署到 GitHub Pages 后，可以通过公开网址访问。

## 数据说明

GitHub Pages 仅提供静态托管，因此页面会使用浏览器本地存储。不同电脑之间的数据不会自动同步。

原服务器的团队共享功能依赖 `/api/data` 接口。如需跨电脑共享同一份数据，需要另外部署后端服务。
